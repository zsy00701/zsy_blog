# 快速添加文章（最简单方式）

## ✅ 是的，只需要推送到 GitHub！

**最简流程（2 步）：**

1. **添加文件到 Git**
   ```bash
   git add content/posts/
   git commit -m "Add new post"
   ```

2. **推送到 GitHub**
   ```bash
   git push
   ```

**完成！** Vercel 会自动：
- ✅ 运行 `normalize-posts`（规范化文章格式）
- ✅ 构建项目
- ✅ 部署上线

等待 1-2 分钟，新文章就会自动上线！

---

## 🤔 为什么不需要手动运行命令？

因为 `package.json` 中配置了 `prebuild` 脚本：

```json
{
  "scripts": {
    "prebuild": "npm run normalize-posts",
    "build": "next build"
  }
}
```

当 Vercel 执行 `npm run build` 时，会自动先运行 `prebuild`，也就是自动规范化所有文章。

---

## 💡 可选：本地预览（推荐但非必需）

如果你想在推送前先看看效果：

```bash
# 规范化文章（可选）
npm run normalize-posts

# 本地预览（可选）
npm run dev
# 访问 http://localhost:3000 查看
```

然后：
```bash
git add .
git commit -m "Add new post"
git push
```

---

## 📋 完整对比

### 方式一：最简单（推荐）

```bash
# 1. 创建文件（用编辑器）
# 2. 提交并推送
git add content/posts/
git commit -m "Add new post"
git push
```

**优点**：最简单，什么都不用管  
**缺点**：看不到规范化后的效果

### 方式二：本地预览后推送

```bash
# 1. 创建文件
# 2. 规范化（可选）
npm run normalize-posts
# 3. 本地预览（可选）
npm run dev
# 4. 提交并推送
git add content/posts/
git commit -m "Add new post"
git push
```

**优点**：可以看到效果，确保无误  
**缺点**：多几步操作

---

## ✅ 总结

**最简单的方式就是：**
1. 创建 `.md` 文件
2. `git add` + `git commit` + `git push`

**其他都是可选的！** 🎉

