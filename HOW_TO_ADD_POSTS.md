# 部署后如何添加新文章

部署完成后，添加新文章非常简单！只需要 3 步。

## 🚀 快速流程（最简单 2 步）

> **重要提示**：你只需要推送到 GitHub，Vercel 会自动运行 `normalize-posts` 和构建，**不需要手动运行任何命令**！

### 第 1 步：在本地添加文章

**方法一：直接创建文件（推荐）**

1. 在 `content/posts/` 目录下创建新的 `.md` 文件
2. 可以按分类放在子文件夹中，例如：
   ```
   content/posts/
   ├── 科研学习/
   │   └── 新文章.md
   └── 生活随笔/
       └── 新文章.md
   ```

3. 文件格式示例：
   ```markdown
   ---
   title: 我的新文章
   date: 2025-01-20
   excerpt: 这是文章的摘要
   category: 科研学习
   tags:
     - 标签1
     - 标签2
   ---

   # 文章标题

   这里是文章内容...
   ```

**方法二：使用脚本创建**

```bash
npm run add-post
```

脚本会引导你输入：
- 标题
- 分类
- 标签
- 摘要
- 内容

### 第 2 步：提交并推送到 GitHub

打开终端，进入博客目录：

```bash
cd /Users/zhoushengyao/blog
```

添加文件并提交：
```bash
git add content/posts/
git commit -m "Add new post: 文章标题"
git push
```

### 完成！✅

**就这么简单！** 推送到 GitHub 后，Vercel 会自动：
1. ✅ 运行 `normalize-posts`（规范化文章格式）
2. ✅ 构建项目
3. ✅ 部署上线（1-2 分钟）

你可以在 Vercel 的 "Deployments" 页面查看部署进度。

> **为什么不需要手动运行命令？**  
> 因为 `package.json` 中配置了 `prebuild` 脚本，Vercel 在构建时会自动运行 `normalize-posts`。

---

## 📝 详细步骤说明

### 场景一：添加单篇文章

**示例：添加一篇关于"Python学习"的文章**

1. **创建文件**
   ```bash
   # 在 content/posts/ 目录下创建
   content/posts/科研学习/Python学习.md
   ```

2. **编写内容**
   ```markdown
   ---
   title: Python 学习笔记
   date: 2025-01-20
   excerpt: 记录 Python 学习过程中的心得体会
   category: 科研学习
   tags:
     - Python
     - 编程
   ---

   # Python 学习笔记

   今天学习了 Python 的基础语法...
   ```

3. **提交并推送**
   ```bash
   git add content/posts/科研学习/Python学习.md
   git commit -m "Add post: Python学习"
   git push
   ```

### 场景二：批量添加多篇文章

1. **添加所有新文件**
   ```bash
   git add content/posts/
   ```

2. **一次性提交**
   ```bash
   git commit -m "Add multiple posts"
   git push
   ```

### 场景三：修改已有文章

1. **直接编辑文件**
   用编辑器打开 `content/posts/` 下的 `.md` 文件修改

2. **提交修改**
   ```bash
   git add content/posts/文件名.md
   git commit -m "Update post: 文章标题"
   git push
   ```

### 场景四：删除文章

1. **删除文件**
   ```bash
   rm content/posts/要删除的文章.md
   ```

2. **提交删除**
   ```bash
   git add content/posts/
   git commit -m "Delete post: 文章标题"
   git push
   ```

---

## 🎯 最佳实践

### 1. 规范化文件（可选但推荐）

添加文件后，运行规范化脚本确保格式正确：

```bash
npm run normalize-posts
```

这个脚本会：
- 自动补全缺失的 frontmatter
- 从文件名提取标题（如果没写）
- 从文件创建时间提取日期（如果没写）
- 生成摘要（如果没写）

### 2. 本地预览

推送前，可以在本地预览：

```bash
npm run dev
```

访问 `http://localhost:3000` 查看效果

### 3. 提交信息规范

使用清晰的提交信息：

```bash
# ✅ 好的提交信息
git commit -m "Add post: Python学习笔记"
git commit -m "Update post: 修正ssh连接文章的错误"
git commit -m "Add 3 posts about machine learning"

# ❌ 不好的提交信息
git commit -m "update"
git commit -m "fix"
```

---

## ⚡ 快速命令参考

```bash
# 1. 添加新文章（使用脚本）
npm run add-post

# 2. 规范化所有文章
npm run normalize-posts

# 3. 本地预览
npm run dev

# 4. 提交并推送
git add content/posts/
git commit -m "Add new post"
git push
```

---

## 🔍 验证部署

### 检查部署状态

1. **Vercel 方式**：
   - 访问 [vercel.com](https://vercel.com)
   - 进入你的项目
   - 查看 "Deployments" 标签
   - 看到最新的部署状态

2. **查看网站**：
   - 等待 1-2 分钟
   - 访问你的博客网站
   - 检查新文章是否出现

### 常见问题

**Q: 推送后网站没有更新？**

A: 检查：
1. 文件是否真的提交了：`git status`
2. 是否推送到远程：`git log origin/main`
3. Vercel 部署日志是否有错误

**Q: 文章显示但格式不对？**

A: 运行规范化脚本：
```bash
npm run normalize-posts
git add content/posts/
git commit -m "Normalize posts"
git push
```

**Q: 想回退到之前的版本？**

A: 在 Vercel 的 "Deployments" 页面，找到之前的部署，点击 "..." → "Promote to Production"

---

## 📋 完整工作流程示例

假设你要添加一篇新文章：

```bash
# 1. 进入项目目录
cd /Users/zhoushengyao/blog

# 2. 创建新文章（或使用编辑器创建）
vim content/posts/科研学习/新文章.md

# 3. 规范化（可选）
npm run normalize-posts

# 4. 本地预览（可选）
npm run dev
# 在浏览器打开 http://localhost:3000 查看

# 5. 提交
git add content/posts/
git commit -m "Add post: 新文章标题"

# 6. 推送
git push

# 7. 等待 1-2 分钟，访问你的网站查看新文章
```

---

## 💡 小贴士

1. **使用 Git 分支**（高级）：
   ```bash
   git checkout -b add-new-post
   # 添加文章...
   git push origin add-new-post
   # 在 GitHub 创建 Pull Request 合并
   ```

2. **批量操作**：
   可以一次性添加多个文件，然后一起提交

3. **定期备份**：
   虽然 Git 已经是备份，但也可以定期导出 `content/posts/` 目录

4. **使用编辑器插件**：
   - VS Code 的 Markdown 预览插件
   - 支持 frontmatter 的编辑器

---

## ✅ 总结

添加新文章的流程就是：

1. **本地创建** → 2. **Git 提交** → 3. **推送到 GitHub** → 4. **自动部署**

就这么简单！🎉

